general:
  build_dir: www

test:
  pre:
    - npm install -g karma
    - npm install -g bower
    - bower install --config.cwd=./angular --config.directory=./www/lib
  override:
    - karma start ./test/unit-tests.conf.js --single-run
