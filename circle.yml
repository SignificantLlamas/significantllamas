test:
  pre:
    # - npm install
    # - npm install -g karma
    - npm install -g bower
    - bower install --config.directory=./www/lib
  override:
    - karma start unit-tests.conf.js --single-run
